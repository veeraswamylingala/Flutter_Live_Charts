import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:live_line_charts_sample/live_charts.dart';

class TagDataTable extends StatefulWidget {
  @override
  _TagDataTableState createState() => _TagDataTableState();
}

class _TagDataTableState extends State<TagDataTable> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Padding(
          padding: const EdgeInsets.all(8.0),
        ));
  }
}
